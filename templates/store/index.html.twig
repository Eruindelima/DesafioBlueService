{% extends 'base.html.twig' %}

{% block title %}Hello StoreController!{% endblock %}

{% block body %}
    {% for produto in produtos %}
        {{ produto.id }}
        {{ produto.nome }}
    {% else %}
        no records found
    {% endfor %}
{% endblock %}
